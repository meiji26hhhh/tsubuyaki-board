# 演習タイムテーブル (講師用・全21時間目)

時間表記は「演習開始から N時間目」で統一する。演習は 0-19時間目、成果まとめは 19-21時間目。物理日は副題として併記。

| 時間目 | 物理日 | フェーズ名 | 受講生の動き | 講師の動き | 想定リスク |
|---|---|---|---|---|---|
| 0h-1h | 1日目 | セットアップ確認 | Doctor 実行、Oracle 起動、初回 `./mvnw -B -Ph2 verify` 緑、Eclipse 自動ビルド OFF | Doctor の NG を 1 人ずつ確認、Pleiades 文字コードを念押し | Pleiades の MS932 文字化け、Podman Desktop 未起動 |
| 1h-2h | 1日目 | 仕様読解＋プロンプト準備 | EXERCISES.md を読み込み、TDD プロンプト雛形を自分用に整える、M1 受入基準を自分の言葉で再記述 | プロンプト読み合わせ、`th:utext` 不使用など規約の念押し | EXERCISES を流し読みして要件を取り違える |
| 2h-4h | 1日目 | 投稿一覧 (M1) | 自分のブランチ `<github-id>`、TDD で Repository → Service → Controller、commit & push | プロンプトのレビュー、生成コードを 1 人ずつ目視 | Codex が `findAll().subList()` で済ませて DataJpaTest が薄い |
| 4h-4.25h | 2日目 | ウォームアップ | doctor --quick、前日コミットのスモークテストとセルフレビュー | 前日コミット品質を確認、再現性 NG を早期発見 | 前日環境差分で起動失敗 |
| 4.25h-6.5h | 2日目 | 投稿作成＋バリデーション (M2/M3) | フォーム描画、バリデーション込みで POST 実装 | Bean Validation の手書きチェック化を阻止 | `@NotBlank` を `if (... == null)` にしがち |
| 6.5h-8h | 2日目 | 投稿詳細＋ヘルスチェック (M4/M5) | 404 ハンドリング、`/actuator/health` 緑、`-Pcoverage-day2` (70%) 緑 | Controller での `Optional.isEmpty()` 早期 return を確認、70% gate 未達を早期検出 | `orElseThrow` で 500 系を出してしまう |
| 8h-9.5h | 2日目 | リファクタ＋カバレッジ80%到達 | JaCoCo 確認、`-Pcoverage-day3 -Pstrict` 緑、Service の Spring 非依存化 | 「Codex が書いたコードを 1 つ選んで日本語で説明する」5 分セッションを挟む | カバレッジ偽装テストを書きがち |
| 9.5h-11.5h | 2日目 | いいね機能 (S1) | Flyway V2、いいねトグルの冪等性、clientHash のハッシュ保存 | IP の生保存を NG として指摘、salt のハードコード阻止 | clientHash の salt をハードコード |
| 11.5h-13h | 2日目 | キーワード検索 (S2) | `?q=` 実装、bind 変数、空文字フォールバック | `@Query` の文字列連結を即時 NG | 文字列連結で SQL を組んで Checkstyle 警告 |
| 13h-13.25h | 3日目 | ウォームアップ | doctor --quick、前日コミットのセルフレビュー | 前日終端のスモークと差分確認 | 朝イチの環境差分 |
| 13.25h-14.5h | 3日目 | 投稿者拡張＋UX微調整 (S3) | アバター色、フォーム改善、エラーハンドラ | UX 微調整と機能追加のコミット分離を確認 | 機能と UI 修正を 1 コミットに混ぜがち |
| 14.5h-17h | 3日目 | COULD選択枠 (C1/C2/C3から1つ) | タグ／論理削除／REST API のいずれかを完了 | 開始時に 3 課題比較ミニガイダンス 10 分、選択判断を支援 | 全部やろうとしてどれも未完で終わる |
| 17h-19h | 3日目 | 仕上げ＋ブランチ整流化＋プロンプトカタログ作成 | コミット整備、`docs/prompts-i-used.md` 作成、ルーブリック自己採点準備 | コミット前セルフチェック観点と prompts-i-used.md の3件以上を確認 | コミットメッセージが「色々修正」、prompts-i-used.md が空 |
| 19h-20h | 3日目 | 相互レビュー | Compare ビュー（`compare/main...<github-id>`）画面共有、隣の人が 3 観点 (機能・テスト・コミット) でコメント | 1 人 1 つだけ「ここ良い」を全体共有 | レビューコメントが「いいね」だけ |
| 20h-21h | 3日目 | KPT＋講評＋自己採点 | KPT、15 点ルーブリックで自己採点 | 「Codex を信用しすぎた / しなさすぎた」のハンドサインで距離感を言語化 | 振り返りが具体的でない |

## 講師の前日チェック (1日目朝・AI講座開始前)

各受講生 PC で以下を踏ませる。

```powershell
# Windows
wsl -l -v                              # WSL2 + Ubuntu が出ること
```

```bash
# WSL
podman ps -a                           # tsubuyaki-oracle が exited でも OK
podman image exists codex-devbox:latest && echo OK
test -n "${OPENAI_API_KEY:-}" && echo "OPENAI_API_KEY 設定済み"
bash /mnt/c/workspace/<repo>/scripts/doctor.sh --quick
```

## 講師の当日持ち物

- 予備 USB に WSL Ubuntu 22.04 イメージ (`wsl --export` で作っておく)
- 予備の OPENAI_API_KEY (1 名分、緊急時に貸し出す)
- ホワイトボード or 画面共有用のラップトップ
- このリポの最新版を `git clone` した手元コピー (`git diff` をデモするため)
- COULD 選択枠 (14.5h 開始時) 用の 3 課題比較表

## 3日通しでの運営原則

1. **0-1h で 30 分以上詰まったら強制的に H2 プロファイルに逃がす**。受講生の DB トラブルで全員を止めない。
2. **2日目朝・3日目朝の 0.25h はウォームアップ専用**。前日コミットのスモークテストで再現性 NG を早期発見する。
3. **2日目終業 (13時間目) で MUST 未完走の受講生はフォロー対象**。3日目朝に H2 強制移行を含む救済策を提示する。
4. **個別質問は時間を切って受ける**。1 件 5 分で打ち切り、長引くものは昼休み or 終業後に。
5. **Codex の操作画面を全員に 1 度ずつ見せてもらう**。早い人を指名してデモにすると、遅い人が真似できる。
6. **`git status` → `git diff` → `git stash push -u` → `git restore <file>` の安全手順を必ず 1 回デモする**。全変更破棄は通常手順にしない。
7. **COULD 選択 (14.5h) の前にミニ全体ガイダンス 10 分**。3 課題の難度比較と選び方を見せる。
8. **17h-19h の仕上げで `docs/prompts-i-used.md` の記入状況を巡回確認**。空のまま 20h を迎えさせない。

## 各日終業時のチェックポイント

| 物理日 | 時間目 | 講師が確認すること |
|---|---|---|
| 1日目終業 | 4h | M1 が自分の `<github-id>` ブランチに commit & push 済みか |
| 2日目終業 | 13h | MUST 5項目 + SHOULD 2項目 (S1/S2) が自分のブランチに入っているか、カバレッジ 80% 到達済みか |
| 3日目演習終了 | 19h | SHOULD 3項目 + COULD 1項目選択 + prompts-i-used.md が揃っているか |
